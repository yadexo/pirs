/**
 * Used only when a clinic has no branding row yet. Matches DEFAULT_CURRENCY's
 * market (lib/currency.ts) and the TenantBranding.timeZone column default.
 */
export const DEFAULT_TIME_ZONE = "Europe/Amsterdam";
